---
deployment:
  tasks:
    - export DEPLOYPATH=/home/techandc/public_html/court/
    - /bin/cp -R * $DEPLOYPATH
    # - /bin/find $DEPLOYPATH -type d -exec chmod 755 {} +
    # - /bin/find $DEPLOYPATH -type f -exec chmod 644 {} +
